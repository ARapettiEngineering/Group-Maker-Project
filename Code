import random  # random used to randomize names


classList = ['Andrew', 'Chris', 'Aidan', 'Ben', 'Liam', 'Brandon', 'Jessie', 'Emma', 'Matt', 'Robert', 'Bobby', 'Brian', 'Alane', 'Anne', 'Evan', 'Eric', 'Jeanette', 'Lucas', 'Spencer', 'Graham']
#classList = []  # insert class list here




#controls inputs for groups to make sure they are valid
while True:
    try:
        group = int(input("Type 2 for groups of 2, 3 for groups of 3 and 4, and 4 for groups of 4: "))
        if group in (2, 3, 4):
            break
        else:
            print("Invalid input for group size. Please try again.")
    except ValueError:
        print("Please enter a number (2, 3, or 4).")




#controls input for exceptions to make sure they are valid
while True:
    try:
        excep = int(input("Type 1 for no exceptions, 2 for an exception: "))
        if excep in (1, 2):
            break
        else:
            print("Invalid input for exceptions. Try again.")
    except ValueError:
        print("Please enter a number (1 or 2).")


#makes sure exceptions are valid and in the class list
while excep == 2:
      exception1 = input("Input First Exception: ").strip().title()
      if exception1 in (classList):
          break
      else:
          print("Invalid input for exception. Try again.")
      print("Please enter exception 1")
while excep == 2:
      exception2 = input("Input Second Exception: ").strip().title()
      if exception2 in (classList):
          break
      else:
          print("Invalid input for exception. Try again.")
      print("Please enter exception 2")
#sets up groups 1-3 for all group sizes
group1 = []
group2 = []
group3 = []
if excep == 2: #sets up groups 4-10 for all group sizes with no exceptions
  if group == 3: #sets up groups 4-6 for groups of 3 and 4 and groups 7-10 for groups of 2
    group4 = []
    group5 = [exception1]
    group6 = [exception2]


  if group == 4: #sets up groups 4-5 for groups of 3 and 4 and groups 6-10 for groups of 2
    group4 = [exception1]
    group5 = [exception2]
    group6 = []
#sets up groups 7-10 for groups of 2
group7 = []
group8 = []
group9 = []
group10 = []


if excep == 2:
  if group == 2: #sets up groups 4-10 for groups of 2
    group4 = []
    group5 = []
    group6 = []
    group9 = [exception1]
    group10 = [exception2]


# Remove exceptions from class list
if excep == 2:
  for exc in (exception1, exception2):
    if exc in classList:
        classList.remove(exc)
    else:
        print(f"Warning: '{exc}' not found in class list")


if excep == 1: #sets up groups 1-10 for all group sizes with no exceptions
    group1 = []
    group2 = []
    group3 = []
    group4 = []
    group5 = []
    group6 = []
    group7 = []
    group8 = []
    group9 = []
    group10 = []


groupNumber = 1
randomPick = []
while len(classList) > 0 and group == 3: #if it is groups of 3 and 4 adds people to groups 1-6 until class list is empty
  randomPick = random.choice(classList)
  if groupNumber == 1:
    group1.append(randomPick)
  if groupNumber == 2:
    group2.append(randomPick)
  if groupNumber == 3:
    group3.append(randomPick)
  if groupNumber == 4:
    group4.append(randomPick)
  if groupNumber == 5:
    group5.append(randomPick)
  if groupNumber == 6:
    group6.append(randomPick)
  classList.remove(randomPick)
  groupNumber = groupNumber + 1
  if groupNumber == 7:
    groupNumber = 1
if group == 3: #prints the groups if it is groups of 3 and 4
  print("Group 1: ", group1)
  print("Group 2: ", group2)
  print("Group 3: ", group3)
  print("Group 4: ", group4)
  print("Group 5: ", group5)
  print("Group 6: ", group6)
while len(classList) > 0 and group == 2: #if it is groups of 2 adds people to groups 1-10 until class list is empty
  randomPick = random.choice(classList)
  if groupNumber == 1:
    group1.append(randomPick)
  if groupNumber == 2:
    group2.append(randomPick)
  if groupNumber == 3:
    group3.append(randomPick)
  if groupNumber == 4:
    group4.append(randomPick)
  if groupNumber == 5:
    group5.append(randomPick)
  if groupNumber == 6:
    group6.append(randomPick)
  if groupNumber == 7:
    group7.append(randomPick)
  if groupNumber == 8:
    group8.append(randomPick)
  if groupNumber == 9:
    group9.append(randomPick)
  if groupNumber == 10:
    group10.append(randomPick)
  classList.remove(randomPick)
  groupNumber = groupNumber + 1
  if groupNumber == 11:
    groupNumber = 1
if group == 2: #prints the groups if it is groups of 2
  print("Group 1: ", group1)
  print("Group 2: ", group2)
  print("Group 3: ", group3)
  print("Group 4: ", group4)
  print("Group 5: ", group5)
  print("Group 6: ", group6)
  print("Group 7: ", group7)
  print("Group 8: ", group8)
  print("Group 9: ", group9)
  print("Group 10: ", group10)
while len(classList) > 0 and group == 4: #if it is groups of 4 adds people to groups 1-5 until class list is empty
  randomPick = random.choice(classList)
  if groupNumber == 1:
    group1.append(randomPick)
  if groupNumber == 2:
    group2.append(randomPick)
  if groupNumber == 3:
    group3.append(randomPick)
  if groupNumber == 4:
    group4.append(randomPick)
  if groupNumber == 5:
    group5.append(randomPick)
  classList.remove(randomPick)
  groupNumber = groupNumber + 1
  if groupNumber == 6:
    groupNumber = 1
if group == 4: #prints the groups if it is groups of 4
  print("Group 1: ", group1)
  print("Group 2: ", group2)
  print("Group 3: ", group3)
  print("Group 4: ", group4)
  print("Group 5: ", group5)



